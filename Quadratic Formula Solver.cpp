#include <iostream>
#include <math.h>

int main() {

    int aTrm{ 0 }; //The a term
    int bTrm{ 0 }; //The b term
    int cTrm{ 0 }; //The c term
    int negB{ 0 }; //The -b +/- sqrt() thing

    std::cout << "Please imput the [a] term\n";
        std::cin >> aTrm;
    std::cout << "Please imput the [b] term\n";
        std::cin >> bTrm;
    std::cout << "Please imput the [c] term\n";
        std::cin >> cTrm;

    negB = aTrm * -1

}
